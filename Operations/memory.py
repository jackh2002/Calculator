def addMemory(x, memory):
    memory += x
    return memory


def removeMemory(x, memory):
    memory -= x
    return memory


def clearMemory(memory):
    memory = 0
    return memory


def showMemory(memory):
    return memory
